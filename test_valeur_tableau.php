	$valeur_id=$id; //valeur test
	$tableau_autorise = array(6,123,12,...);
	if (in_array($valeur_id,$tableau_autorise)) {		
     $valider = $valeur_id; //autoriser tableau
	}else{		
	$valider = '6'; //si valeur standard  
	}
